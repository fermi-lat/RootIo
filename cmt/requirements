package RootIo
version v4r3

branches src doc

use GlastSvc v8r*
use Event v8r*
use ROOT v3r2p*
use mcRootData v2r2
use digiRootData v3r4
use reconRootData v3r0p2
use GlastPolicy v4r*

apply_pattern packageShr

# uses for test program: make private here 
# the private use breaks linux build
# private
use G4Generator v4r*
use FluxSvc v5r*
use GuiSvc v3
use AcdDigi v1r*
use TkrDigi v1r*
use CalDigi v1r*
use CalRecon v5r*
use TkrRecon v6r*


apply_pattern package_headers

library RootIo  *.cxx Dll/*.cxx ${RootIo_headers}

ignore_pattern package_stamps



apply_pattern package_Cshlibflags

private


# build this for testing
macro jobOptions "" VisualC "test/jobOptions.txt"

public
set JOBOPTIONS ${ROOTIOROOT}/src/test/jobOptions.txt

private
application test_RootIo ${GlastMain} test/*.cxx $(jobOptions)

macro test_RootIolinkopts  "  $(GuiSvc_lib) $(guiapp_linkopts) $(gui_linkopts) -u GuiSvc_loadRef  -ldl "\
      WIN32 " ${guiapp_linkopts} $(GuiSvc_lib)  /include:_GuiSvc_loadRef "









